generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Product {
  id         Int      @id @default(autoincrement())
  name       String
  pricePerKg Float
  stock      Float
  imageUrl   String
  category   String
  createdAt  DateTime @default(now())
}
